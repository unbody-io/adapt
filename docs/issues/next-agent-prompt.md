# Task: Implement `brain.update()` Redesign

Read `docs/specs/brain-update.spec.md` for the full spec and implement it. Use `src/learners/text-learner/class.ts:288` (`learner.update()`) as the pattern to follow.

Type check with `npx tsc --noEmit`. Update `evals/scripts/brain-08-brain-update.ts` to verify the new behavior.

Out of scope: `createLearner()` duplication fix, UI dropdown wiring.
