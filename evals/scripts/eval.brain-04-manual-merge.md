# brain-04-manual-merge

**Purpose:** Tests `brain.mergeNeurons()` for combining multiple neurons with overlapping domains into one.

**Evaluates:**
- Two neurons with overlapping domains merge into one
- Source neurons are deleted after merge
- Merged neuron retains combined knowledge

**Parameters:**
- Model: configurable via `MODEL` env var
