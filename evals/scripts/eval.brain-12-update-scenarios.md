# brain-12-update-scenarios

**Purpose:** Tests 5 sequential `brain.update()` scenarios on a single brain instance.

**Evaluates:**
- Additive prompt, combo update, threshold cascade, subtractive prompt, full pivot

**Parameters:**
- Model: configurable via `MODEL` env var
- Evolution: enabled, signal threshold 1, autoEvaluate off
