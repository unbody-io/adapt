# learner-01-lifecycle

**Purpose:** Tests learner creation from config and initialization within a Brain.

**Evaluates:**
- Creating a learner via `brain.createLearnerFromConfig()`
- Learner state after initialization
- Event emission during lifecycle

**Parameters:**
- Model: configurable via `MODEL` env var
- Learner type: text, continuous governance
