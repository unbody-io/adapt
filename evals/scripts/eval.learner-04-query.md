# learner-04-query

**Purpose:** Tests `learner.query()` — context retrieval and response synthesis.

**Evaluates:**
- Response quality against known understanding
- Confidence scoring
- Context retrieval from stored understanding

**Parameters:**
- Model: configurable via `MODEL` env var
- Learner type: text, continuous governance
