# Model Capabilities
Tests 5 capability categories against a given model: text output, structured output (JSON schema), tool calling, structured output + tool calling, reasoning (thinking deltas). Useful for validating provider/model compatibility.
## Config
PROVIDER and MODEL env vars control target model.
