# neuron-01-lifecycle

**Purpose:** Tests neuron creation from config and initialization within a Brain.

**Evaluates:**
- Creating a neuron via `brain.createNeuronFromConfig()`
- Neuron state after initialization
- Event emission during lifecycle

**Parameters:**
- Model: configurable via `MODEL` env var
- Neuron type: text, continuous governance
