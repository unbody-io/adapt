# neuron-04-query

**Purpose:** Tests `neuron.query()` — context retrieval and response synthesis.

**Evaluates:**
- Response quality against known understanding
- Confidence scoring
- Context retrieval from stored understanding

**Parameters:**
- Model: configurable via `MODEL` env var
- Neuron type: text, continuous governance
