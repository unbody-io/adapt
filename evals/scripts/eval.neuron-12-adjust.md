# neuron-12-adjust

**Purpose:** Exercises `neuron.adjust(directive)` across additive, subtractive, pivot, and behavioral scenarios.

**Evaluates:**
- Additive, subtractive, pivot, behavioral adjust directives
- Data preservation after adjust
- List neuron adjust compatibility

**Parameters:**
- Model: configurable via `MODEL` env var
- Neuron types: text and list
