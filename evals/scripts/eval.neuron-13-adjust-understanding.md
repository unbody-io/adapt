# neuron-13-adjust-understanding

**Purpose:** Tests `adjust()` with understanding-targeting directives on both text and list neurons.

**Evaluates:**
- Classification routing (config vs understanding vs both)
- Understanding changes when directive targets knowledge
- Text editing and list CRUD tools

**Parameters:**
- Model: configurable via `MODEL` env var
- Neuron types: text and list
