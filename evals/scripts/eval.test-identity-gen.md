# Identity Generation Test
Tests observe and understand identity generation across multiple models (Gemini, Claude, GPT-4o-mini). Verifies LLM produces valid identity schemas for different instruction prompts.
