# Two-Phase Learning Eval
TextLearner eval with two-phase learning (observe → synthesize) using continuous strategy. Produces a human-reviewable markdown report. Accepts dataset and batch-size as CLI args.
